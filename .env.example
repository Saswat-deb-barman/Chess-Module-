VITE_GOOGLE_CLIENT_ID=
VITE_COUNCIL_URL=http://localhost:8787
# Optional — only needed if the Socket.io backend is on a different host
# than the council backend above. Falls back to VITE_COUNCIL_URL if unset.
VITE_SOCKET_URL=
